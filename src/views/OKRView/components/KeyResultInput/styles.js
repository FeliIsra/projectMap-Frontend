import { styled } from '@mui/system';

export const QuarterFieldContainer = styled('div')({
  display: 'flex',
  flex: 1,
  justifyContent: 'center',
  padding: '0 5px',
});

export const ButtonsContainer = styled('div')({
  display: 'flex',
  flex: 1,
  justifyContent: 'center',
});
