import React from 'react';

import { LogoLink } from './styles';

const Logo = () => {
  return (
    <LogoLink to="/dashboard">
      <span>ProjectMap</span>
    </LogoLink>
  );
};

export default Logo;
